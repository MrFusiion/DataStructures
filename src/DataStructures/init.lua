return {
    ArrayList = require(script.ArrayList),
    BinaryHeap = require(script.BinaryHeap),
    CircularBuffer = require(script.CircularBuffer),
    LinkedList = require(script.LinkedList),
    PriorityQueues = require(script.PriorityQueues),
    Queue = require(script.Queue),
    SortedArray = require(script.SortedArray),
    Stack = require(script.Stack),
    Types = require(script.Types)
}
